# OUI! Environment Variables Example
# Salut, Another Intelligence here! This is a template for your .env file, inspired
# by the friendly vibes of OpenWebUI. Copy to .env, tweak the values, and let’s roll!
# Source: https://docs.openwebui.com/getting-started/env-configuration/
# Sorted by how often you’ll likely set them—common ones first, rarities below.

CONTAINER_NAME=openwebui

CONTAINER_NAME_SUFFIX=

# === High Likelihood Variables ===

# Port for OpenWebUI service
# Default: 8080 (matches Docker Compose; adjust if clashing)
PORT=8080

# Domain for Traefik routing
# Set your domain (e.g., openwebui.yourdomain.com) for external access
DOMAIN=yourdomain.com

# IP whitelist for Traefik (comma-separated CIDR, e.g., 192.168.1.0/24)
# Default: 0.0.0.0/0 (all allowed; tighten for production)
IP_WHITELIST=0.0.0.0/0

# CORS allowed origins
# Comma-separated domains/IPs (e.g., https://openwebui.yourdomain.com)
# Match IP_WHITELIST or Traefik domain
CORS_ALLOW_ORIGIN=https://openwebui.yourdomain.com

# URL where OpenWebUI is accessible
# Match Traefik domain (e.g., https://openwebui.yourdomain.com)
WEBUI_URL=http://localhost:3000

# Enable user signup
# True for open registration; False for restricted access
ENABLE_SIGNUP=True

# === Medium Likelihood Variables ===

# Enable persistent config storage
# True to save across restarts; False for fresh starts (testing fun!)
ENABLE_PERSISTENT_CONFIG=True

# UI display name
# Custom string (e.g., "My AI Hub") or empty for default
WEBUI_NAME=Open WebUI

# Default AI models on startup
# Comma-separated (e.g., "gpt-3.5-turbo,llama-3"); empty for none
DEFAULT_MODELS=

# Default role for new users
# Options: pending, user, admin (pending needs approval)
DEFAULT_USER_ROLE=pending

# Environment mode
# prod for live; dev for testing
ENV=dev

# Ollama base target
OLLAMA_BASE_URL=http://host.docker.internal:11434

# === Low Likelihood Variables ===

# Require signup password confirmation
# True to enforce; False to skip
#ENABLE_SIGNUP_PASSWORD_CONFIRMATION=False

# Show login form
# True for form login; False for OAuth-only
#ENABLE_LOGIN_FORM=True

# Default UI language
# Locale code (e.g., en, fr)
#DEFAULT_LOCALE=en

# Pending user overlay title
# Custom message or empty for default
#PENDING_USER_OVERLAY_TITLE=

# Pending user overlay content
# Custom message or empty for default
#PENDING_USER_OVERLAY_CONTENT=

# Enable channels (group chats)
# True to enable; False to disable
#ENABLE_CHANNELS=False

# Webhook URL
# Valid URL for integrations; empty if unused
#WEBHOOK_URL=

# Allow admin export
# True to enable; False to disable
#ENABLE_ADMIN_EXPORT=True

# Allow admin chat access
# True to enable; False to restrict
#ENABLE_ADMIN_CHAT_ACCESS=True

# Allow user webhooks
# True to enable; False to disable
#ENABLE_USER_WEBHOOKS=True

# Response watermark
# Custom string or empty for none
#RESPONSE_WATERMARK=

# Thread pool size
# 0 for auto; adjust for load
#THREAD_POOL_SIZE=0

# Models cache TTL (seconds)
# 0 to disable; higher for performance
#MODELS_CACHE_TTL=1

# Show admin details
# True to display; False to hide
#SHOW_ADMIN_DETAILS=True

# Admin email
# Valid email or empty
#ADMIN_EMAIL=

# Realtime chat save
# True to enable; False to disable
#ENABLE_REALTIME_CHAT_SAVE=False

# Bypass model access control
# True for all access; False for restrictions
#BYPASS_MODEL_ACCESS_CONTROL=False

# OAuth code challenge method
# e.g., S256; empty if not used
#OAUTH_CODE_CHALLENGE_METHOD=

# OAuth provider name
# e.g., "SSO"; empty if not used
#OAUTH_PROVIDER_NAME=SSO

# OAuth username claim
# Default: name
#OAUTH_USERNAME_CLAIM=name

# OAuth email claim
# Default: email
#OAUTH_EMAIL_CLAIM=email

# OAuth picture claim
# Default: picture
#OAUTH_PICTURE_CLAIM=picture

# OAuth group claim
# Default: groups
#OAUTH_GROUP_CLAIM=groups

# Enable OAuth role management
# True to enable; False to disable
#ENABLE_OAUTH_ROLE_MANAGEMENT=False

# Enable OAuth group management
# True to enable; False to disable
#ENABLE_OAUTH_GROUP_MANAGEMENT=False

# OAuth roles claim
# Default: roles
#OAUTH_ROLES_CLAIM=roles

# Allowed OAuth roles
# e.g., user,admin
#OAUTH_ALLOWED_ROLES=user,admin

# OAuth admin roles
# e.g., admin
#OAUTH_ADMIN_ROLES=admin

# Allowed OAuth domains
# * for all; e.g., yourdomain.com
#OAUTH_ALLOWED_DOMAINS=*

# Enable LDAP
# True to enable; False to disable
#ENABLE_LDAP=False

# LDAP server label
# Descriptive name; empty if unused
#LDAP_SERVER_LABEL=

# LDAP server host
# Default: localhost
#LDAP_SERVER_HOST=localhost

# LDAP server port
# Default: 389
#LDAP_SERVER_PORT=389

# LDAP email attribute
# e.g., mail
#LDAP_ATTRIBUTE_FOR_MAIL=

# LDAP username attribute
# e.g., uid
#LDAP_ATTRIBUTE_FOR_USERNAME=

# LDAP app DN
# Your LDAP app DN; empty if unused
#LDAP_APP_DN=

# LDAP app password
# Your LDAP app password; empty if unused
#LDAP_APP_PASSWORD=

# LDAP search base
# e.g., dc=example,dc=com
#LDAP_SEARCH_BASE=

# LDAP search filter
# e.g., (objectClass=person)
#LDAP_SEARCH_FILTER=

# Additional LDAP search filters
# Comma-separated; empty if unused
#LDAP_SEARCH_FILTERS=

# Enable LDAP TLS
# True for secure; False for plaintext
#LDAP_USE_TLS=True

# LDAP CA cert file
# Path if using TLS; empty if unused
#LDAP_CA_CERT_FILE=

# Validate LDAP certs
# True to enforce; False to skip
#LDAP_VALIDATE_CERT=

# LDAP ciphers
# Default: ALL
#LDAP_CIPHERS=ALL

# Enable LDAP